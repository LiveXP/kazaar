<div class="table-responsive">
    <table class="table table-hover">
        <thead>
        <tr>
            <th>Check</th>
            <th>Résultat</th>
            <th>Commentaire</th>
        </tr>
        </thead>
        <tbody>
        {% for checking in morningCheck.checkings %}
            <tr>
                <td>{{ checking.name }}</td>
                <td class="{{ checking.status is null ? '' : checking.status.color }}">
                    {{ checking.status is null ? "Non rempli" : checking.status.name }}
                </td>
                <td>
                    {{ checking.comment|md2html }}
                    {% for image in checking.images %}
                        <a class="img-preview" href="{{ image|image }}">
                            <img class="margin-bottom20" src="{{ image|image }}">
                        </a>
                    {% endfor %}
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
</div>
